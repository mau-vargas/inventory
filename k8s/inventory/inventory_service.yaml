apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: testing
spec:
  selector:
    app: inventory-fastapi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30001
  type: NodePort
