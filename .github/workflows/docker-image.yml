name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
   
    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: "ing.mauvargas@gmail.com"
        password: "maufunk1"   
    - name: Build the Docker image
      run: |
        cd app/
        docker build -t inventory-fastapi:0.3 .
        docker tag inventory-fastapi:0.3 mauvargas/inventory-fastapi:0.3
        docker push mauvargas/inventory-fastapi:0.3