apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-deployment
  namespace: testing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:14-alpine
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        - name: init-sql
          mountPath: /docker-entrypoint-initdb.d/init.sql
          subPath: init.sql
      volumes:
      - name: postgres-data
        emptyDir: {}
      - name: init-sql
        hostPath:
          path: /Users/mauriciovargas/Desktop/Projects/Python/Proyecto_2/inventory/init.sql  # Reemplazar con la ruta absoluta del archivo init.sql en el host
          type: File

